<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rxee.backend.mapper.UserMapper">
    <select id="findAll" resultType="com.rxee.backend.entity.User">
        select * from sys_user
    </select>

    <insert id="insert" parameterType="com.rxee.backend.entity.User">
        insert into
            sys_user(username,
                     password,
                     nickname,
                     email,
                     phone,
                     address)
             values(#{username},
                    #{password},
                    #{nickname},
                    #{email},
                    #{phone},
                    #{address})
    </insert>

    <update id="update" parameterType="com.rxee.backend.entity.User">
        update
            sys_user
        <set>
            <if test="username != null">username=#{username},</if>
            <if test="password != null">password=#{password},</if>
            <if test="nickname != null">nickname=#{nickname},</if>
            <if test="email != null">email=#{email},</if>
            <if test="phone != null">phone=#{phone},</if>
            <if test="address != null">address=#{address}</if>
        </set>
        where
            id=#{id}
    </update>

    <delete id="deleteById" parameterType="com.rxee.backend.entity.User">
        delete from
            sys_user
        where
            id=#{id}
    </delete>
</mapper>
